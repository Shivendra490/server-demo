<%- include('../includes/head.ejs') %>
   
</head>

<body>
    <%- include('../includes/navigation.ejs') %>

    <main>
        <%if(orders.length<=0){%>
            Nothing here in orders
        <%} else {%>
            <%orders.forEach((order)=>{%>
                #<%=order.id%>
                <%order.products.forEach(product=>{%>
                <li><%=product.title%> (<%=product.orderItem.quantity%>)</li>
                <%})%>
            <%})%>  
          <%}%>    
          
    </main>
<%- include('../includes/end.ejs') %>